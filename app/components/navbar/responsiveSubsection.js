import React from 'react';
import FeatureSubMenu from './featuresubmenu';

function ResponsiveSubsection({ subsectionData }) {
  return <FeatureSubMenu data={subsectionData} />;
}

export default ResponsiveSubsection;
